export const GridSize = {
  rows: 25,
  columns: 25,
};

export const RefreshIntervalMilliseconds = 3000;

export const DefaultValues = {
  MIN_AVAILABLE_PLAYER_TIME: 1, // in minutes
  MAX_TURN_DURATION: 120, // in seconds
};
